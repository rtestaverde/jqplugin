(function ($){
	$.fn.toSvgLetterSoupMaker = function(options){
		var cnfg = $.extend( {
			columns: 20,
			rows: 20,
			words: null,
			labels: {
				buttons: {
					save: 'Save',
					reset: 'Reset'
				}
			}
		}, options);
		return $(this).each(function(){
			var el = $(this);
			
			
			
			return el;
		});	
	}
})(jQuery);